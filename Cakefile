Cakefile.new do |c|

        c.application_for :ios, 8.0 do |t|

          t.name = "Sup"
          t.include_files = ["./**/*.*", "./**/*"]
          t.exclude_files = "./**/*.rb"

          t.all_build_configurations.settings["INFOPLIST_FILE"] = "Info.plist"
        end

end
